package com.tupackage.chefkiss.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales ChefKiss
val CoralPrimary = Color(0xFFFF6B4A)  // Naranja Coral Gourmet
val TechBlue = Color(0xFF4A90E2)       // Azul Tecnológico

// Colores de fondo y superficie
val Background = Color(0xFFFEFEFE)
val Surface = Color(0xFFFFFFFF)
val SurfaceVariant = Color(0xFFF8F8FA)

// Colores de texto
val TextPrimary = Color(0xFF2C2C2C)
val TextSecondary = Color(0xFF717182)

// Colores de acento
val AccentLight = Color(0xFFFFF4F1)  // Rosa muy suave para glassmorphism
val AccentBorder = Color(0x14000000)  // Borde sutil (rgba(0,0,0,0.08))

// Colores funcionales
val Success = Color(0xFF34D399)
val Warning = Color(0xFFFBBF24)
val Error = Color(0xFFEF4444)

// Amarillo para estrellas de rating
val StarYellow = Color(0xFFFBBF24)

// Transparencias para glassmorphism
val GlassWhite = Color(0xCCFFFFFF)  // 80% opacidad
val GlassBlack = Color(0x80000000)   // 50% opacidad
// 50% opacidad